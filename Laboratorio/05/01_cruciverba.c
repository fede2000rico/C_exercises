#include<stdio.h>
#include<stdlib.h>
char **matrix_in;

int main(){
  FILE *fp;
  fp=fopen("cruciverba_file.txt","r");
  while(!feof(fp)){
    int static r,c;
    

    matrix_in=malloc(sizeof(char));
  }
}
